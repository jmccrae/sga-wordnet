[project]
name = "sga-wordnet"
version = "0.1.0"
description = "A wordnet for Old Irish, built by extension from Open English Wordnet"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "teanga>=0.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.0",
]
